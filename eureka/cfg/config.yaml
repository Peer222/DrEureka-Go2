defaults:
  - _self_
  - env: forward_locomotion_go2
  - override hydra/launcher: local
  - override hydra/output: local

hydra:
  job:
    chdir: True

seed: 0

# LLM parameters
port: 8000
model_path: /bigwork/nhwpduep/master_thesis/models/
model: cmd_line_argument  # original: gpt-4-0125-preview
thinking_enabled: True  # for Qwen3 models with thinking
include_thinking_in_prompt: False # not working for vision and evolved versions (only reward code included)

max_completion_tokens: 10000
use_custom_params: False
temperature: 0.6
top_p: 0.95
top_k: 20
min_p: 0.0
presence_penalty: 0.0
repetition_penalty: 1.0

# Eureka parameters
iteration: 5               # how many iterations of Eureka to run
sample: 16                 # number of Eureka samples to generate per iteration
feedback_series_size: 10   # Number of values included in feedback prompt per metric
fps: 25                    # video feedback
max_video_length: 10       # Maximum length of video in seconds (taken from end)

topk_sampling: 8
num_examples: 2
curriculum_target: False

optimization_target: "fitness_score_last"  # for v-eureka: critic_score, fitness_score_max or fitness_score_last
prompt_collection: "default" # default, minimal, evolved, curriculum

# GPU setup
use_submitit: True         # set in submitit scripts (if set latter options are ignored)
num_gpus: 1                # for more GPUs fix setFreestGPU() call (gpu_stat file not found) (depricated)
processes_per_gpu: 2       # 3 execution failed in dry run and code stuck (original code probably had enough gpus to run all samples per iteration in parallel) (depricated)

# Weights and Biases
use_wandb: True            # whether to use wandb for logging
use_run_wandb: False       # whether to use wandb for logging of individual evaluation runs
wandb_username: "peer222-luh"  # wandb username if logging with wandb
wandb_project: "peer222-luh/master-thesis"  # wandb project if logging with wandb

# resume eureka run from intermediate result
resume: False
stats_file: cmd_line_argument